{
  "name": "@wb-calc/config",
  "version": "1.0.0",
  "description": "Единая система конфигурации для WB Marketplace Calculator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "validate": "node -e \"const { configLoader } = require('./dist/index.js'); configLoader.load().then(() => console.log('✅ Конфигурация валидна')).catch(console.error)\"",
    "generate-schema": "node -e \"const { configSchemas } = require('./dist/index.js'); console.log(JSON.stringify(configSchemas, null, 2))\" > schema.json"
  },
  "keywords": [
    "config",
    "configuration",
    "validation",
    "zod",
    "typescript"
  ],
  "dependencies": {
    "dotenv": "^16.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "src",
    "schema.json"
  ]
}
