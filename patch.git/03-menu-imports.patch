diff --git a/app/client/components/layouts/Menu.tsx b/app/client/components/layouts/Menu.tsx
index 9g0f1e4..c3d4e5f 100644
--- a/app/client/components/layouts/Menu.tsx
+++ b/app/client/components/layouts/Menu.tsx
@@ -1,15 +1,15 @@
-// path: src/components/layouts/Menu.tsx
+// path: app/client/components/layouts/Menu.tsx
 // Neumorphic вертикальное меню навигации со ссылками на основные разделы.

 import React from "react";
 import { NavLink } from "react-router-dom";

-import { cn } from "@/lib/utils";
+import { cn } from "@/client/lib/utils";

 const links = [
   { to: "/", label: "Главная" },
   { to: "/search", label: "Поиск" },
   { to: "/history", label: "История" },
   { to: "/calculator", label: "Калькулятор" },
   { to: "/logs", label: "Логи" },
 ];

-const Menu: React.FC = () => (
+const Menu: React.FC = () => (
   <nav>
     <ul className="space-y-2">
-      {links.map(({ to, label }) => (
+      {links.map(({ to, label }) => (
         <li key={to}>
           <NavLink
             to={to}
             className={({ isActive }) =>
               cn(
                 "block px-3 py-2 rounded-md transition-colors",
                 isActive ? "bg-primary text-primary-foreground" : "hover:bg-accent"
               )
             }
           >
-            {label}
+          {label}
           </NavLink>
         </li>
       ))}
-    </ul>
+  </nav>
   </nav>
 );
