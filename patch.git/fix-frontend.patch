diff --git a/package.json b/package.json
index 1234567..89abcde 100644
--- a/package.json
+++ b/package.json
@@ -6,10 +6,12 @@
-    "dev": "tsx watch --clear-screen=false --no-warnings app.ts",
+    "dev": "tsx -r tsconfig-paths/register watch --clear-screen=false --no-warnings app.ts",
     "frontend": "vite",
     "wait": "wait-on tcp:3000",
-    "dev:all": "npx kill-port 3000 3001 4000 && concurrently -k -n BACKEND,FRONTEND -c green,cyan \"npm run dev\" \"npm run wait && npm run frontend\""
+    "dev:all": "npx kill-port 3000 3001 4000 && concurrently -k -n BACKEND,FRONTEND -c green,cyan \"npm run dev\" \"npm run wait && npm run frontend\""
   },
   "dependencies": {
+    "tsconfig-paths": "^4.2.0"
   }
 }
diff --git a/tsconfig.json b/tsconfig.json
index abcdef0..1234567 100644
--- a/tsconfig.json
+++ b/tsconfig.json
@@ -10,6 +10,16 @@
     "strict": true,
     "esModuleInterop": true,
     "skipLibCheck": true,
-    "forceConsistentCasingInFileNames": true
+    "forceConsistentCasingInFileNames": true,
+    "baseUrl": "./app",
+    "paths": {
+      "@config/*": ["config/*"],
+      "@utils/*": ["utils/*"],
+      "@services/*": ["services/*"],
+      "@models/*": ["models/*"],
+      "@controllers/*": ["controllers/*"],
+      "@middlewares/*": ["middlewares/*"],
+      "@routes/*": ["routes/*"]
+    }
   }
 }
