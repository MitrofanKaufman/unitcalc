import type { MenuItem } from '../components/layout/ResponsiveHeader';
declare const menuItems: MenuItem[];
export declare const getMenuItems: (userRole?: string | null) => MenuItem[];
export default menuItems;
